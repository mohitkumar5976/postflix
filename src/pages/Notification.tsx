import NotificationHeader from "../components/webComponents/Notifcation/NotificationHeader";
import NotificationList from "../components/webComponents/Notifcation/NotificationList";

const Notification = () => {
  return (
    <>
      <NotificationHeader />
      <NotificationList/>
    </>
  );
};

export default Notification;
